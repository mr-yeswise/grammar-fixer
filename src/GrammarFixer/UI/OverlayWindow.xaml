<Window x:Class="GrammarFixer.UI.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GrammarFixer Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Focusable="False"
        IsHitTestVisible="True">
  <Border Background="#F0FFFFFF"
          BorderBrush="#CCCCCC"
          BorderThickness="1"
          CornerRadius="8"
          Padding="10,8"
          Effect="{StaticResource ShadowEffect}">
    <StackPanel>
      <!-- Diff area -->
      <TextBlock x:Name="DiffTextBlock"
                 FontSize="13"
                 TextWrapping="Wrap"
                 MaxWidth="420"
                 Margin="0,0,0,8"/>

      <!-- Action chips -->
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button x:Name="AcceptAllBtn"
                Content="✓ Accept all"
                Click="AcceptAll_Click"
                Margin="0,0,6,0"
                Padding="8,4"
                FontSize="12"
                Background="#2ECC71"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand"/>
        <Button x:Name="DismissBtn"
                Content="× Dismiss"
                Click="Dismiss_Click"
                Padding="8,4"
                FontSize="12"
                Background="#E74C3C"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand"/>
      </StackPanel>
    </StackPanel>
  </Border>
</Window>
